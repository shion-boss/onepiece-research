requests>=2.31
beautifulsoup4>=4.12
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27
pytest>=8.0
anthropic>=0.40
# Vercel 本番で Neon Postgres 接続用。 ローカル開発では DATABASE_URL 未設定 →
# SQLite fallback で動くので、 ローカルに psycopg がインストール不要でも可。
# Vercel build context で必ず install されるよう requirements.txt に記載。
psycopg[binary]>=3.1
